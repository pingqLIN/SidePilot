{
  "name": "sidepilot",
  "version": "1.0.0",
  "description": "SidePilot - Chrome extension with GitHub Copilot integration",
  "scripts": {
    "test": "jest --no-coverage",
    "test:watch": "jest --watch --no-coverage",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/chrome": "^0.0.252",
    "jest": "^29.7.0"
  }
}
